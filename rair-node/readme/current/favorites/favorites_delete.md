# Delete a favorite token

**URL** : `/api/favorites/:id`
**Method** : `DELETE`

**Parameters:**
```json
{
    "id": { "required": true , "content": { "type": "string" } },
}
```

## Success Response
**Code** : `204 NO CONTENT`